using ArrayOperations;

internal class Program
{
    private static void Main(string[] args)
    {
        int[] array = { 1, 2, 3, 4, 5 };
        var rotate = ArrayOperation.RotateArray(array, 2);
        Console.WriteLine(2 % 5);

    }
}